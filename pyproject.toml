[project]
name = "workout-relay"
version = "0.1.0"
description = "Mobile-first structured workout delivery for Garmin Connect"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py310"
